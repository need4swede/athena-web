# ------------------------------
# Athena Web – VPS Deployment
# Copy this file to .env and adjust values
# ------------------------------

# Timezone
TZ=America/Los_Angeles

# Database
DB_HOST=postgres
DB_PORT=5432
DB_NAME=athena_db
DB_USER=athena
DB_PASSWORD=change-me-strong

# Networking
BACKEND_PORT=36464

# App URLs (set to your public domain)
FRONTEND_URL=https://athena.example.com
ALLOWED_ORIGINS=https://athena.example.com

# JWT
JWT_SECRET=replace-with-strong-random

# Feature flags
ENABLE_GOOGLE_NOTES=false
ENABLE_DB_ADMIN=true
DB_AUTO_INIT=true

# Vite note: The frontend image is built with API URL baked in.
# Ensure your published frontend image points to your backend via /api proxy
# (nginx in image proxies /api -> backend:36464).

# Optional SSO – leave blank to disable
SSO_MICROSOFT_CLIENT_ID=
SSO_MICROSOFT_CLIENT_SECRET=
SSO_MICROSOFT_TENANT_ID=organizations

SSO_GOOGLE_CLIENT_ID=
SSO_GOOGLE_CLIENT_SECRET=

SSO_GITHUB_CLIENT_ID=
SSO_GITHUB_CLIENT_SECRET=

SSO_LINKEDIN_CLIENT_ID=
SSO_LINKEDIN_CLIENT_SECRET=

SSO_TWITTER_CLIENT_ID=
SSO_TWITTER_CLIENT_SECRET=

# Limit access (comma-separated). Leave blank to allow all.
SSO_ALLOWED_DOMAINS=
SSO_ALLOWED_EMAILS=

# Aeries (optional)
AERIES_ACCOUNT_FILE=

# Google API (optional)
GOOGLE_SCOPES=["https://www.googleapis.com/auth/admin.directory.user","https://www.googleapis.com/auth/admin.directory.device.chromeos"]
GOOGLE_SERVICE_ACCOUNT_FILE=
GOOGLE_ADMIN_EMAIL=

# Optional: Add headers when fetching JSON keys via URL
ACCOUNT_FILE_HEADERS={}

# Database bootstrap
# With DB_AUTO_INIT=true, the backend image runs /app/database/init.sql automatically on first boot.
